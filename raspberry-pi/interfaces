# /etc/network/interfaces

auto wlan0

iface lo inet loopback
iface etho0 inet dhcp

allow-hotplug wlan0
iface wlan0 inet dhcp
  wpa-ssid "$INSERT_WIFI_NETWORK_NAME_HERE"
  wpa-psk "$INSERT_WIFI_PASSWORD_HERE"
